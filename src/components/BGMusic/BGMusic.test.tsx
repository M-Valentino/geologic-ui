import React from "react";
import { render } from "@testing-library/react";

import BGMusic from "./BGMusic";

describe("BGMusic", () => {
  test("renders the BGMusic component", () => {
    render(<BGMusic />);
  });
});